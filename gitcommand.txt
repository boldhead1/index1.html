
learning git basic command. kind of easy but need a lots of practice.

git status

git commit -m "commit with message"

git commit -am "add and commit"

git add (add to remote repo)

git push (push repo)

git pull

git create branch -b "branch name"

